# Core dependencies
google-generativeai>=0.3.0
numpy>=1.24.0
pillow>=10.0.0
opencv-python>=4.8.0
pytesseract>=0.3.10
pyautogui>=0.9.54
keyboard>=0.13.5
pywin32>=306; sys_platform == 'win32'
requests>=2.31.0
python-socketio>=5.10.0

# Win11 specific UI automation
pygetwindow>=0.0.9
pyperclip>=1.8.2
pywinauto>=0.6.8

# Optional dependencies for advanced features
torch>=2.0.0; platform_system != "Darwin" or platform_machine != "arm64"
torchvision>=0.15.0; platform_system != "Darwin" or platform_machine != "arm64"
# Note: For Apple Silicon, users need to install torch and torchvision separately

# Logging and utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
pyyaml>=6.0.0

# UI (tkinter is generally included with Python)
# If using a Python distribution without tkinter, you may need to install it separately

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0