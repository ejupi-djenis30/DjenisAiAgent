# Minimal dependencies for Docker (web mode only — no Windows UI automation)
# Generated from pyproject.toml [web] + [browser] + core groups

# Core runtime (local LLM access uses Python's standard HTTP client)
Pillow>=10.0.0
python-dotenv>=1.0.0

# Web server
fastapi>=0.141.1
uvicorn[standard]>=0.51.0
websockets>=13.0
python-multipart>=0.0.26
pydantic>=2.12.5

# Browser automation (headless Chrome via Selenium)
selenium>=4.21.0
